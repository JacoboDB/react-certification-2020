// import React from 'react';
// import { render, screen } from '@testing-library/react';

// import VideoItem from './VideoItem.component';

// import { mockData } from '../../data/mock-videos';
// const videoData = mockData.items[0];

// test('test VideoItem component', () => {
//   render(<VideoItem video={videoData} />);
//   const videoItemComponent = screen.getByTestId('video-item');
//   expect(videoItemComponent).toBeInTheDocument();
//   expect(videoItemComponent).toHaveClass('video-item');
//   expect(videoItemComponent).toBeVisible();
// });
